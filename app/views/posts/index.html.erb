<% content_for(:title, "Posts") %>

<%= render partial: 'shared/filter_form', locals: { destination_url: posts_path } %>

<% @posts.each do |post| %>
  <%= render partial: 'post', locals: { post: post } %>
<% end %>

<%= will_paginate @posts, :page_links => false %>
