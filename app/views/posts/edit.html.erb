<% content_for(:title, "Edit #{@post.date}") %>

<%= link_to 'Back', posts_path, class: button_class('secondary float-right ml-2') %>

<h1>Editing Post</h1>

<%= render 'form', post: @post %>
<hr>
<%= link_to 'Delete', @post, method: :delete, data: { confirm: 'Are you sure?' }, class: button_class('danger float-right ml-2') %>
