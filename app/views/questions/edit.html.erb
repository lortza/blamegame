<% content_for(:title, "Edit #{@question.date}") %>

<%= link_to 'Back', questions_path, class: button_class('secondary float-right ml-2') %>

<h1>Editing Question</h1>

<%= render 'form', question: @question %>
<hr>
<%= link_to 'Delete', @question, method: :delete, data: { confirm: 'Are you sure?' }, class: button_class('danger float-right ml-2') %>
