<% content_for(:title, "Editing #{@question.text}") %>


<header>
  <h1>Editing Question</h1>
  <%= link_to 'Back', questions_path, class: "ml-2 #{button_class('secondary')}" %>
</header>

<%= render 'form', question: @question %>
