<div class="card mb-3 <%= post.post_type.name.downcase %>-card">
  <div class="card-header">
    <%= post.post_type_name %> <%= post.date %>
    <% if post.bookmarked? %>
      <i class="fas fa-bookmark float-right ml-2"></i>
    <% end %>
    <span class="float-right ml-2"><%= link_to 'edit', edit_post_path(post), class: button_class('btn btn-sm btn-light float-right ml-2') %></span>
  </div>
  <div class="card-body">
    <%= image_tag(post.image, class: 'image', title: post.date) if post.image.present? %>
    <% if post.with_people.present? %>
      <p class="card-title">with <%= post.with_people %>...</p>
    <% end %>
    <%= markdown(post.description) %>
    <small class="text-muted">
      <%= display_categories(post) %>

      <%= link_to 'related info', post.url, target: '_blank', class: 'float-right ml-2' if post.url.present? %>
    </small>
  </div>
</div>
